import React from "react";

const ShowMore = ({showItems}) => {
  return <>

    <button className="newtodo-container__btn mt-3 mb-3" onClick={showItems}>Show More</button>
  </>;
};

export default ShowMore;
